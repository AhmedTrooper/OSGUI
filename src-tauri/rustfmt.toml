edition = "2021"
max_width = 100
use_small_heuristics = "Default"
# The following options require nightly rustfmt; we use stable so they're
# omitted here. They would otherwise emit warnings every `cargo fmt`.
# imports_granularity = "Crate"
# group_imports = "StdExternalCrate"
# reorder_imports = true
